# SpInfer SpMM Phase 1 Prompt



You are working in a task implementation workspace. Your job is to produce the best correct implementation for the task described below. The target machine is NVIDIA RTX Pro 6000, and the software environment is CUDA 12.8.



## Kernel Information



- **Definition Kernel name:** `SpMM_Kernel_bitmap_v4`

- **Baseline solution name:** `SpMM_Kernel_bitmap_v3`

- **Definition API:** `SpMM_SplitK_API_bitmap_v4`

- **Baseline API:** `SpMM_SplitK_API_bitmap_v3`

- **args:**

    - `cudaStream_t stream`

    - `const half*  A`

    - `const half*  Compressed_A`

    - `const int*   TileOffsets`

    - `const int* TileOffsets_Median`

    - `const uint64_t* bitmap`

    - `const int* max_nnz_intile`

    - `const half*  B`

    - `half*        C`

    - `const int    M_Global`

    - `const int    N_Global`

    - `const int    K_Global`

    - `half*        Reduction_Workspace`

    - `int          Split_K`

The API should launch kernel with different config according to N_Global like `SpMM_SplitK_API_bitmap_v3`, bitmap and offsets information are generated by `InitSparseMatrixA_bitmap`.



## Task Contract



- **Task name:** Optimize SpMM Kernel with TMA and Warp Specialization.

- **Objective:** Optimize a sparse-dense matrix multiplication (SpMM) CUDA kernel by integrating Tensor Memory Accelerator (TMA) for data movement and restructuring it to a producer-consumer model using warp specialization to achieve higher performance through overlapped execution, while maintaining extensibility for future non-zero element quantization.

- **Allowed implementation approaches:** Use KernelWiki for research on Blackwell/Pro6000, CUDA 12.8, PTX inline assembly, `cp.async.bulk` and TMA instructions (`cp.async.bulk.tensor`), `mbarrier` synchronization primitives, warp specialization, and double-buffering for TMA-loaded data.

- **Profile:** Use ncu-report-skill when profiling or interpreting Nsight Compute reports.

- **Validation command:** `bash tests/correction_tests/validate.sh`

- **Benchmakr command:** `bash tests/performance_tests/bench.sh`

- **Promotion criteria:**
  1.  All validation tests pass.
  2.  No `Illegal barrier arrive` or `Missing wait` errors reported by `compute-sanitizer`.



## Official Acceptance

All validation tests pass.



## Phase 1 Goal



Research existing CUDA TMA and warp-specification implementations and produce the first correct Pro6000 implementation. Focus on understanding the Decode + GEMM pipeline in baseline solution. Performance matters, but correctness and a clean baseline design are the priority for this phase.

Before implementing, write an implementation-plan draft and save it to:

```
KDA/docs/phase1-draft.md
```

Prepare to run `/humanize:gen-plan` on that draft to generate the detailed implementation plan.